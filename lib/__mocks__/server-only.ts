// Vitest stub for the `server-only` package.
// In the real Next.js runtime this throws when imported from a client bundle.
// Under vitest (plain Node) we simply do nothing — tests run server-side anyway.
export {}
